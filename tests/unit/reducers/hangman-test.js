import reducersHangman from '../../../reducers/hangman';
import { module, test } from 'qunit';

module('Unit | Reducer | reducers/hangman');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reducersHangman();
  assert.ok(result);
});
